<p id="notice"><%= notice %></p>

<h1>Records</h1>

<table>
  <thead>
    <tr>
      <th>Day</th>
      <th>Height</th>
      <th>Weight</th>
      <th>BMI</th>
      <th>Daycalorie</th>
      <th>Highblood</th>
      <th>Lowblood</th>
      <th>Diary</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |record| %>
      <tr>
        <td><%= record.day %></td>
        <td><%= record.height %></td>
        <td><%= record.weight %></td>
        <td><%= record.bmi %></td>
        <td><%= record.daycalorie %></td>
        <td><%= record.highblood %></td>
        <td><%= record.lowblood %></td>
        <td><%= record.diary %></td>
        <td><%= record.picture %></td>
        <td><%= link_to 'Show', record %></td>
        <td><%= link_to 'Edit', edit_record_path(record) %></td>
        <td><%= link_to 'Destroy', record, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Record', new_record_path %>
